use crate::bootstrap::app_context::AppContext;

pub struct QueryBuilder {
    ctx: AppContext,
}

impl QueryBuilder {
    pub fn new(ctx: AppContext) -> Self {
        Self { ctx }
    }

    pub fn fetch() {

    }
}
